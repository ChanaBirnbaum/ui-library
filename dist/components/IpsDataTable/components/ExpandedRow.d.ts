import { default as React } from 'react';
import { ExpandedRowProps } from '../IpsDataTable.types';

export declare function ExpandedRow({ subData, subColumns, customContent, title, indent, }: ExpandedRowProps): React.JSX.Element;
